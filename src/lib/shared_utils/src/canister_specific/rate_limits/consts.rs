/// Default maximum requests per window for registered users
pub const DEFAULT_MAX_REQUESTS_PER_WINDOW_REGISTERED: u64 = 5;

/// Default maximum requests per window for unregistered users
pub const DEFAULT_MAX_REQUESTS_PER_WINDOW_UNREGISTERED: u64 = 1;

/// Default window duration in seconds (24 hours)
pub const DEFAULT_WINDOW_DURATION_SECONDS: u64 = 86400;